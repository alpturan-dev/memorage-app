:{$PORT} {
    root * /app/dist
    file_server

    # Disable access logs or use minimal format
    log {
        output discard
    }

    # SPA fallback - serve index.html for client-side routing
    try_files {path} /index.html
}
